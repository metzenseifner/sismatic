# API Reference

The entire public surface of the `sismatic` package, rendered from the type
stub. The instruction-name unions (`QueryName`, `CommandName`, `RegisterName`)
list every spelling accepted at runtime.

::: sismatic
